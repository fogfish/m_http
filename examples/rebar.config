{erl_opts, []}.

{deps, [
   {datum, ".*",
      {git, "https://github.com/fogfish/datum", {branch, generic}}
   }

,  {m_http, ".*",
      {git, "https://github.com/fogfish/m_http", {branch, master}}
   }
]}.

{profiles, [
   {test, [{deps, [meck]}]}
]}.
